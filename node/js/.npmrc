# The @cube-drone marquee packages live on public npm. The home ~/.npmrc routes the scope to
# GitHub Packages (for publishing, over in marqueemarkup); this project-level override keeps
# installs here pointed at the registry the lockfile already pins.
@cube-drone:registry=https://registry.npmjs.org/
