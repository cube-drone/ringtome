/target
**/*.rs.bk
*.pdb

# local node data (per-user databases, key files, materialized views)
/data
**/data/

# env / secrets
.env
*.env

# scratch: transcoder test output, dumped for eyeballing (see media.rs corpus tests)
/scratch

# node / js integration tests
node_modules/

