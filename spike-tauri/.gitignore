src-tauri/target/
# Regenerated by tauri-build on every build (permission schemas for plugins we do not use).
src-tauri/gen/schemas/
# Copies of in-repo sources, rebuilt by ./sync-vendor.sh - never edit here, edit the originals.
ui/vendor/
# Encoded artifacts the video probe saves for cross-checking against the Rust decoders.
out/
